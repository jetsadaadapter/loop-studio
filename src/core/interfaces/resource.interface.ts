/**
 * Core resource types and contracts.
 * Types must be derived from Zod validators — do NOT define types manually here.
 * Import from validators instead.
 */
export type { AppResource, ResourceType as ResourceTypeValue } from "@/core/validators/resource.validator";
